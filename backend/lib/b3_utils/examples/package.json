{
  "name": "encrypted-notes-dapp",
  "version": "0.2.0",
  "keywords": [
    "Internet Computer",
    "Motoko",
    "Svelte",
    "Canister",
    "Docker",
    "Rust"
  ],
  "scripts": {
    "dfx:start": "sh dfx-start.sh",
    "generate": "dfx generate",
    "dev": "cd vetkd && npm run dev",
    "build": "cd vetkd && npm install && npm run build",
    "deploy": "cargo test && dfx deploy",
    "test": "cargo test"
  }
}
